app:
  name: "Realtime Chat"
  env: "development" # development | staging | production
  port: 1234

db:
  host: "localhost"
  port: 3306
  user: "root"
  pass: "your_password_here"
  name: "chat_service"

redis:
  host: "localhost"
  port: 6379
  pass: "" # empty means no password
  db: 0 # redis logical database index

jwt:
  secret: "REPLACE_WITH_A_LONG_RANDOM_SECRET"
  expiry: "1h"

# Messaging layer
kafka:
  enabled: false # future use
  brokers: []
  clientId: "realtime-chat-service"
