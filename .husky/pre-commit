#!/usr/bin/env sh

echo "🔍 Linting..."
pnpm lint:stage || exit 1

echo "🧪 Running unit tests..."
pnpm test:unit || exit 1

echo "📦 Building..."
pnpm build || exit 1

echo "✅ Pre-commit passed!"
