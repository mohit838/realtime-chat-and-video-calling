version: "3.9"
services:
  api:
    build: .
    container_name: chat-api
    restart: unless-stopped
    ports:
      - "5050:5000"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
